trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: AzurePowerShell@5
  inputs:
    azureSubscription: 'myconnection'
    ScriptType: 'FilePath'
    ScriptPath: '/ResourceGroupTest/Deploy-AzTemplate.ps1'
    ScriptArguments: -Location 'centralus' -ResourceGroupName 'demogroup'
    azurePowerShellVersion: 'LatestVersion'
